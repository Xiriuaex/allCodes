// Program to exchange the position of strings in array using array of pointers.
#include <iostream>

using namespace std;

int main()
{
    char *v1[] = {"Luffy", "Zoro", "Nami"};
    for (int i = 0; i < sizeof(v1[3]); i++)
    {
        int len = sizeof(*v1[i]);
    }

    for (int i = 0; i < sizeof(v1[3]); i++)
    {
        for (int i = 0; i < sizeof(); i++)
        {
            /* code */
        }

        cout << *v1[i];
        cout << endl;
    }
}